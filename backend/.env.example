PORT=5001
NODE_ENV=development

MONGO_URI=

JWT_SECRET=
JWT_REFRESH_SECRET=

# Comma-separated list of allowed frontend origins (no trailing slash), e.g.
# CLIENT_URLS=http://localhost:5173,https://citytrack.vercel.app
# Leave empty locally to allow any origin. Set this in production —
# credentialed CORS cannot use '*'.
CLIENT_URLS=

GEMINI_API_KEY=
